import { FC } from 'react';
import { layoutLogo } from '@component/layoutLogo.module.less';

const LayoutLogo: FC<unknown> = () => {
  return <div className={layoutLogo}></div>;
};

export default LayoutLogo;
